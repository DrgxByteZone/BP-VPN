<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network & VPN Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Query installed apps for Per-App Firewall -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".BPApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_bp_panther_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bp_panther_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BPVPN">

        <!-- Main Dashboard & Splash Host Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Security Subsystems Activities -->
        <activity
            android:name=".feature.splittunneling.ui.SplitTunnelActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.dnsshield.ui.DnsShieldActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.securityaudit.ui.SecurityAuditActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.watchdog.ui.AdvancedSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Low-Level Android VPN Service -->
        <service
            android:name=".vpn.service.BPVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="On-device network packet firewall and security tunnel" />
        </service>

        <!-- Quick Settings Status Bar Tile -->
        <service
            android:name=".system.BPQuickSettingsTile"
            android:exported="true"
            android:icon="@drawable/ic_bp_panther_logo"
            android:label="@string/quick_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Boot Completed Receiver for Auto-Shield -->
        <receiver
            android:name=".system.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Network Change Receiver -->
        <receiver
            android:name=".system.NetworkChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
